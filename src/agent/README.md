# Agent 层说明

这个目录预留给后续 Agent 核心能力实现。

当前阶段属于 `Phase 0`，这里只建立目录边界，不提前堆业务逻辑。

后续计划放置的能力包括：

- `core.js`
  负责组合模型、记忆和工具
- `memory.js`
  负责短期对话记忆
- `loop.js`
  负责任务执行循环

当前状态：

- 目录已建立
- 等待 `Phase 1 / Phase 2 / Phase 3` 逐步落地
